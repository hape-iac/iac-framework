# Stack: private-vm

Creates one VM and configures IAP + OS Login access.
Default mode is private VM without external IP.
Public VM mode is supported with `enable_public_ip=true` and `use_public_subnet=true`.
