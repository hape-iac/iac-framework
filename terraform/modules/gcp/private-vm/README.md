# Module: private-vm

Creates a VM with configurable subnet profile and optional external IP, and configures IAM-based SSH access with IAP and OS Login.
Implementation files are added after explicit stack-generation approval.
